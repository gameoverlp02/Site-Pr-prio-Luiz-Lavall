{
  "version": 2,
  "buildCommand": "echo 'done'",
  "outputDirectory": ".",
  "framework": null,
  "routes": [
    { "handle": "filesystem" },
    { "src": "/projeto/(.*)", "dest": "/projeto.html" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
